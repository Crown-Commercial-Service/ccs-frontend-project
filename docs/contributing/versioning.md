# Versioning

We follow [Semantic Versioning](https://semver.org/) so new versions should be marked as follows:
  - Breaking changes corresponds to a major (1.X.X) change.

  - New features correspond to a minor (X.1.X) change.

  - Fixes correspond to a patch (X.X.1) change.
