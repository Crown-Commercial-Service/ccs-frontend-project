const { axe } = require('./axe')
const { render, renderMacro } = require('./render')
const { getExamples } = require('./getExamples')
const { htmlWithClassName } = require('./htmlWithClassName')

module.exports = {
  axe,
  render,
  renderMacro,
  getExamples,
  htmlWithClassName
}
